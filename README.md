# web_dev
hello , this repository is for all html and css files

git commands 

1 git status  -- it shows the updated or created or deleted files 
2 git add     -- it adds the untracked files 
3 git commit -m "" -m ""    -- it commit a file with a message written inside the first qoutes and some details in the second qoutes